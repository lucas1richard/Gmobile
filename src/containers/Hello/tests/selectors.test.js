// import { fromJS } from 'immutable';
// import { makeSelectHelloDomain } from '../selectors';

// const selector = makeSelectHelloDomain();

describe('makeSelectHelloDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
